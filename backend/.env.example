NODE_ENV=development
PORT=5000
MONGODB_URI=mongodb://localhost:27017/astu_complaint_system
JWT_SECRET=your_jwt_secret_key_here_change_in_production
JWT_EXPIRE=7d
CORS_ORIGIN=http://localhost:3000,http://127.0.0.1:3000,http://localhost:5173,http://127.0.0.1:5173
SMTP_SERVICE=gmail
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_app_password_here
SMTP_FROM=noreply@astu.edu.et
SMTP_TLS_REJECT_UNAUTHORIZED=true
FRONTEND_URL=http://localhost:5173
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-1.5-flash
CHAT_HISTORY_LIMIT=12
MAX_FILE_SIZE=5242880
ALLOWED_FILE_TYPES=image/jpeg,image/png,application/pdf

# Email Setup Instructions:
# 1. For Gmail: Enable 2FA and create an App Password at https://myaccount.google.com/apppasswords
# 2. For Outlook: Use your regular password or create an App Password
# 3. For custom SMTP: Set SMTP_HOST, SMTP_PORT, leave SMTP_SERVICE empty
# 4. Set SMTP_USER to your email address
# 5. Set SMTP_PASS to your app password or regular password
# 6. Set SMTP_FROM to the sender email address (can be same as SMTP_USER)
